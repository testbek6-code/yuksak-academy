services:
  - type: web
    name: yuksak-academy-web
    env: python
    region: singapore
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -c website/gunicorn_config.py website.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.11
